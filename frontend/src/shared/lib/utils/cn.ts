/**
 * `cn` – class name merge helper.
 * Combines clsx (conditional classes) with tailwind-merge (deduplication).
 * Re-exported from shared/lib to avoid duplicating the import across the codebase.
 */
export { cn } from '@/lib/utils';
